companyCode: TTIC
state: FL
product: AF3
type: coverageLimit
name: increasedCompliance
version: '1970-01-01T00:00:00.000Z'
details:
  name: increasedCompliance
  readonly: true
  amount: "${it.coverageLimits.increasedCompliance.value}"
  displayText: Increased Cost of Compliance
  initialValue: "${['B', 'C', 'X'].includes(it.property.floodZone) ? 4 : ['A', 'V'].includes(it.property.floodZone) && it.property.yearBuilt < 1975 ? 55 : it.property.floodZone === 'A' ? 4 : 13}"
  groups:
  - coverage
  - initialData
